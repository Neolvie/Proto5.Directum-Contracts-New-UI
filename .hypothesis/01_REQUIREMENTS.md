## Business Analyst — REQUIREMENTS
**Дата/время:** 2026-01-21 12:05  
**Фаза:** Уточнение требований

**Кратко:** Зафиксированы функциональные/нефункциональные требования прототипа AI Contract Assistant, подтверждены ответы пользователя.

### Контекст и допущения
- Реальный LLM endpoint доступен; параметры берутся из `.env`.
- Режим полного ответа — plain text с цитатами и ссылками на страницы (без форматирования).
- Кастомные проверки хранятся локально в `localStorage`.
- Рейтинги сохраняются на сервере с различением пользователей по IP.
- Docker Desktop доступен; Playwright используется для E2E.
- UI должен быть максимально похож на Directum Omni и конфигурируем.

### Функциональные требования (FR)
1. Выбор роли при первом запуске (3 роли) с сохранением в `localStorage` и восстановлением при последующих визитах.
2. Загрузка 1–5 документов (DOCX, XLSX, PDF, MD), каждый до 10 МБ.
3. Отображение преднастроенных проверок по роли:
   - Запуск проверки нажатием кнопки (в чат отправляется промт).
   - Редактирование текста и промта существующих кнопок.
   - Создание новых кнопок с кастомными проверками.
4. «Улучшить промт» для новой/редактируемой проверки через LLM.
5. Свободный ввод вопроса пользователем.
6. Три режима детализации ответа: краткий, расширенный, полный (с цитатами и ссылками на страницу).
7. Система рейтинга ответа (👍/👎) с отправкой на сервер.
8. Локальное хранение кастомных проверок в `localStorage`.
9. Отображение списка загруженных документов.

### Нефункциональные требования (NFR)
1. Простая и интуитивная UI/UX без визуального шума.
2. Обработка документов в пределах разумного времени на типовых файлах.
3. Безопасность: .env не коммитится; ключи не выводятся в логи.
4. Логирование рейтингов и запросов возможно на сервере (без PII кроме IP).
5. Код на Python 3.10+ с типизацией и докстрингами на русском.
6. Тесты: pytest (unit/integration), Playwright (E2E), покрытие >= 70%.
7. Docker: сборка и запуск обязательны.

### Пользовательские сценарии (UC)
1. Пользователь заходит впервые → выбирает роль → роль сохраняется.
2. Загружает 1–5 документов → список отображается в UI.
3. Нажимает стандартную проверку → в чат отправляется запрос → получает ответ.
4. Меняет режим ответа → повторяет запрос → получает ответ в нужном формате.
5. Создает/редактирует проверку → сохраняет в `localStorage` → использует.
6. Ставит оценку 👍/👎 → рейтинг отправляется на сервер с IP.

### Критерии приемки
- Роль сохраняется и восстанавливается между сессиями.
- Загружаются файлы DOCX/XLSX/PDF/MD (1–5, <=10 МБ).
- Кнопки проверок по роли отображаются и работают.
- Кастомные проверки создаются/редактируются/удаляются и сохраняются в `localStorage`.
- Ответы формируются в 3 режимах, «полный» включает цитаты и страницы.
- Рейтинги сохраняются на сервере с фиксацией IP.
- Прототип запускается локально и в Docker.
- Все тесты и E2E проходят, coverage >= 70%.

**Следующие шаги:**
- Подготовить архитектуру и выбрать стек UI/Backend.
- Определить структуру модулей и API.

**Вопросы/блокеры:** нет
